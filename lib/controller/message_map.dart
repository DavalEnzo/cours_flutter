import 'package:flutter/material.dart';

class MessageMap extends StatelessWidget {
  const MessageMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
